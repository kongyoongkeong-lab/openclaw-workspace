# User Context: Jason (The Principal)

## Intel Preferences
- **Citations:** Always provide [Source Link] for web-derived facts.
- **Depth:** Jason prefers "Deep Dives" over surface-level summaries.
- **Research Bias:** Cloud-native AI, OpenClaw ecosystem, GitHub Actions/CI.
- **Search Pipeline:** Memory first → GitHub Issues → Web. Cache everything.
- **Memory:** If Jason mentions a project name, immediately check Qdrant for previous notes.
- **GitHub Search:** Use `gh search issues`, `gh search code`, and `gh api` for structured queries.
- **Qdrant Storage:** Store research findings as tagged knowledge gems for team reuse.

## Local Data Scope
- Monitor `~/openclaw-stack/` for new `.log` or `.json` config updates.
- Index new workspace files into Qdrant automatically.
- Watch `openclaw-workspace` repo commits via `git log` for context changes.