# 2026-05-22

- First wake-up in this OpenClaw workspace.
- Human: Jason.
- Assistant identity: RTX4078, an AI assistant.
- Working vibe: practical, direct, lightly dry; helpful without ceremony.
- Signature emoji: ⚡.

## Runtime Baseline Upgrade

- Established a clean Git baseline for the OpenClaw workspace on branch `main`.
- Local commits created:
  - `f6882c5 Add hybrid model runtime baseline`
  - `659adba Add context-resilient hybrid routing`
  - `53cf91f Add GitHub-governed API context budgeting`
- GitHub remote remains `https://github.com/kongyoongkeong-lab/openclaw-workspace.git`; do not push without Jason's explicit approval because the remote is public and old workspace backup material may contain unmanaged runtime files.
- Clean tracked baseline includes:
  - `.gitignore`
  - `config/model_runtime.json`
  - `docs/model_runtime_strategy.md`
  - `docs/github_governance.md`
  - `memory/decisions/openclaw.md`
  - `memory/preferences/jason.md`
  - `memory/system/stable_rules.md`
  - `shortcuts.md`
  - `tools/model_mode_state.py`
  - `tools/model_runtime_status.py`
  - `tools/model_route_decision.py`
  - `tools/api_context_budget.py`
- Live workspace files, agent folders, installed skills, daily memory, caches, runtime logs, and secrets stay untracked/local unless Jason explicitly requests otherwise.

## Model Runtime Policy

- Active model setup mode is `hybrid`.
- Setup modes:
  - `hybrid`: default; API orchestrates long-context/tool-heavy work, local Ollama handles fallback/private/compact tasks.
  - `api`: API-first for long-context, tool-heavy, high-reliability work.
  - `local`: Ollama-first for compact/private work or when API is unavailable.
- Local Ollama models detected:
  - `qwen3.5:9b` primary local reasoning fallback.
  - `qwen3.5:4b` degraded/faster worker.
  - `qwen3.5:2b` minimal router/summary fallback.
  - `llava:latest` local vision.
  - `llava:7b-v1.5-q4_K_S` alternate local vision.
  - `nomic-embed-text` embeddings.
- GPU baseline: NVIDIA GeForce RTX 4070 SUPER, 12 GB class VRAM, observed 12,282 MiB.
- Local inference ceiling encoded around 9,728 MiB / 9.5 GB safety class; check VRAM before local LLM or image generation work.

## Context Resilience

- Prior context overflow issue was addressed by encoding explicit context pressure controls.
- Context thresholds:
  - 50%: summarize/offload before continuing.
  - 65%: keep current-step context only and reload exact files as needed.
  - 80%: hard stop expansion, write handoff summary, resume from artifacts.
- API model context budgeting now uses both official model spec and observed runtime cap.
- Current API model in runtime state: `openai-codex/gpt-5.5`.
- Official GPT-5.5 context window recorded as 1,000,000 tokens.
- Observed OpenClaw runtime cap recorded as 272,000 tokens.
- Effective current API budget uses the lower of model spec and runtime cap:
  - effective window: 272,000 tokens
  - usable input budget: 217,600 tokens
  - output reserve: 32,640 tokens
  - reasoning reserve: 21,760 tokens
  - target fill: 239,360 tokens
  - soft compact: 204,000 tokens
  - hard compact: 244,800 tokens
  - absolute stop: 261,120 tokens
- Intent: maximize API context according to model/runtime spec while preserving overflow protection.

## Shortcut / Tooling Notes

- `API模型`, `本地模型`, and `混合模型` are backed by `tools/model_mode_state.py`.
- `当前模型` should summarize active model/fallback/quota/routing state via runtime status.
- `模型策略` should summarize `docs/model_runtime_strategy.md`.
- `tools/model_runtime_status.py` reports GPU/Ollama/runtime policy.
- `tools/model_route_decision.py` recommends API/local routing under context/API/GPU availability constraints.
- `tools/api_context_budget.py` computes API context budgets from model spec plus observed runtime cap.

## Verification Notes

- JSON config validation passed after upgrades.
- Routing simulations passed for normal API use, API unavailable fallback, and context pressure warnings.
- Staged secret scans were clean for committed files.
- `git diff --check` passed during setup.
- A global OpenClaw config outside the staged repo set was reported by scanner as containing a high-risk token; do not expose or commit it.

## Runtime Remediation Pass

- Jason confirmed proceed at 2026-05-22 21:58+08:00 for controlled remediation after security/runtime audit.
- Remediation report written to `reports/audit/protocol-remediation-2026-05-22.md`.
- Runtime config plaintext gateway and Telegram credentials were migrated to SecretRefs backed by `~/.openclaw/secrets/runtime.json` with `0600` permissions; no secret values were printed.
- Generated `models.json` plaintext `providers.codex.apiKey` residue was removed during the pass, but later audit found it can regenerate and should be handled via an OpenClaw-supported regeneration/SecretRef path.
- `openclaw secrets reload --json` passed with `ok=true` and `warningCount=0`.
- `openclaw secrets audit --check` reported `plaintext=0`, `unresolved=0`, `shadowed=0`; two OAuth legacy residues remain out of scope for static SecretRef migration.
- `openclaw doctor --fix` ran successfully and migrated default model refs from `openai-codex/gpt-5.5` to `openai/gpt-5.5` while preserving Codex runtime routing.
- Doctor repaired two stale session routes and disabled unavailable skills: `api-gateway`, `clawhub`, `local-whisper`.
- Added GPU preflight script `tools/check_gpu.sh`; live check passed around 1.6 GiB VRAM used, below the 9,728 MiB ceiling.
- Added protocol mode script `tools/protocol_mode_state.py` and source-of-truth files `PROTOCOL_INVARIANTS.md`, `CURRENT_RUNTIME.md`, and `PROVIDER_STATUS.md`.
- Updated context budget reporting to the current 1,000,000-token session window.
- Open follow-ups: set `commands.ownerAllowFrom` only after Jason provides exact channel/user id; set Telegram group sender allowlist only after exact allowed sender ids; decide separately whether to quarantine/remove duplicated `self-evolving-skill-1.0.2` copies flagged for `child_process`; decide separately whether to disable `gateway.controlUi.allowInsecureAuth`; investigate doctor's inconsistent "Gateway token SecretRef unavailable" warning despite successful secrets reload/audit.
- No GitHub push, merge, pull, Qdrant write, production activation, workflow change, new agent, telemetry expansion, or persistent background task was performed.

## Agent Coordination / Skill Deep Audit

- Jason requested at 2026-05-22 22:06+08:00 a deep audit of each agent role, coordination, and skills, with GitHub improvement reference.
- Audit was read-only and report written to `reports/audit/agent-coordination-skill-audit-2026-05-22.md`.
- Current runtime evidence showed only `main` active, no active child subagents, and four workspace agent folders: `intel`, `ops`, `comms`, `sentinel`.
- Main finding: Pentagon roles are documented, but specialization is weak; all four subagents share 23 common skills.
- `@intel` and `@comms` have no unique role-specific skill surface; `@ops` and `@sentinel` only have a few unique skills.
- Coordination gap: `agent_registry.json` is referenced by docs/instructions but is missing.
- Tool schema mismatch: `SOUL.md` describes `sessions_send(agent="sentinel", message="...")`, but loaded OpenClaw tool schema appears to require different addressing; fix should be explicit and tested before relying on A2A delegation.
- Stale role policy found: `ops` still referenced 90%+ GPU utilization; `sentinel` still referenced RTX 4070 Ti Super / 98%, conflicting with the current RTX 4070 SUPER baseline and 9,728 MiB safety ceiling.
- Security audit still has critical findings, especially five duplicated `self-evolving-skill-1.0.2` copies using `child_process`, plus missing Telegram group sender allowlist.
- GitHub improvement reference: issue `#12` and branch `master` contain useful protocol-audit ideas, but `master` is cloud-native-only and CI still fails markdown lint plus secret scan; do not merge directly.
- Recommended next controlled pass: audit-only hardening first by creating `agent_registry.json`, adding a hybrid-compatible `scripts/protocol_audit.py`, and normalizing subagent role files to current invariants; leave skill pruning/security boundary closure for explicit separate confirmation.
